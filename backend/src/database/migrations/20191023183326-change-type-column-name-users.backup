module.exports = {
  up: (queryInterface, Sequelize) => {
    return queryInterface.renameColumn('users', 'name', 'person_id', {
      type: Sequelize.INTEGER,
      references: { model: 'persons', key: 'id' },
      unique: true,
      onUpdate: 'CASCADE',
      onDelete: 'SET NULL',
      allowNull: true,
    });
  },

  down: (queryInterface, Sequelize) => {
    return queryInterface.renameColumn('users', 'person_id', 'name', {
      type: Sequelize.STRING,
      allowNull: false,
    });
  },
};
